#pragma once

namespace tomo
{
  namespace fdm
  {
    namespace routing
    {
      namespace functor
      {
        template<typename PATH>
        struct Shift
        {
        };
      }

      template<typename IN, typename POINT, typename OUT>
      void shift(const IN 
  }
}
