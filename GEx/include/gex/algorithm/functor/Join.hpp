#pragma once

#include <gex/strategy/join.hpp>

namespace gex
{
  namespace algorithm
  {
    namespace functor
    {
      template<typename IN, typename OUT, typename STRATEGY>
      struct Join {};
    }
  }
}
